import { DonaImagePipe } from './dona-image.pipe';

describe('DonaImagePipe', () => {
  it('create an instance', () => {
    const pipe = new DonaImagePipe();
    expect(pipe).toBeTruthy();
  });
});
